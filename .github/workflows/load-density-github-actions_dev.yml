name: LoadDensity GitHub Actions Dev

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

permissions:
  contents: read

jobs:
  build_dev_version:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.7
        uses: actions/setup-python@v3
        with:
          python-version: "3.7"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel
          pip install -r dev_requirements.txt
      # unit test start
      - name: fast http user test
        run: python ./test/unit_test/user_test/fast_http_user_test.py
      - name: multi action user test
        run: python ./test/unit_test/user_test/multi_action_user_test.py
